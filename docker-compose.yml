services:
  frontend:
    build:
      context: ./starbucks-map
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - "4200:4200"
    volumes:
      - ./starbucks-map:/app
      - /app/node_modules